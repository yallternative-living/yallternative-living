# Dev-time only -- never ships to the deployed site, and never needs
# to be committed. Installed via `npm install` for scripts/optimize-images.js.
node_modules/
package-lock.json

# OS cruft
.DS_Store

# AI Agent runtime state & logs
.agents/
.Jules/
*.log

# Stray editor/filesystem lock artifacts -- harmless leftovers, not real
# source files, sandbox permissions prevented deleting them outright.
check_tmp.js
assets/data/.fuse_hidden*
assets/data/shop_jsonld.tmp.json
*.tmp

# Local reports
audit_report.md
scripts/etsy-sync-report.md
*audit-report.md
*audit_report.md
