{
  "_readme": [
    "Approved inline <script> blocks for this site's Content-Security-Policy.",
    "",
    "scripts/build-security-headers.js hashes every inline script on every page",
    "(all the top-level HTML files plus every generated products/*.html) and",
    "refuses to build if it finds one that is not listed here. Without that",
    "check the generator re-hashed whatever it found on each deploy, so any",
    "script that reached a page -- including text typed into the CMS at /admin",
    "and interpolated into the Tawk.to snippet by build-site-data.js -- was",
    "automatically allowlisted by the very policy meant to contain it. That is",
    "findings C-4 and H-13 in docs/AUDIT-2026-09-01.md.",
    "",
    "WHEN THE BUILD FAILS ON A NEW HASH: read the script it prints. Confirm",
    "line by line that it is a change you or a colleague made on purpose. Only",
    "then add its hash below, with a note saying what the script does and which",
    "file emits it, in the same commit as the change itself. Never paste in a",
    "hash to make a red build go green.",
    "",
    "A hash listed here that matches nothing on the site is reported as stale",
    "on every build and is left out of the emitted CSP; delete it once you are",
    "sure the script is gone for good."
  ],
  "hashes": {
    "sha256-Wmlc/Lumj50wOdax6AEbaQv0zXK/9D0NZCQxPTdXDqM=": {
      "what": "No-flash theme init: reads the yl-theme preference and sets data-theme before first paint.",
      "emittedBy": "hand-written in each page's <head>",
      "pages": "all 15 top-level pages"
    },
    "sha256-GwShjQS8ayLbH/DuImQmgPbWdnTGaE3Khi+i/XB9D38=": {
      "what": "Tawk.to live-chat loader, deferred. Loads the widget SDK on the visitor's first pointerdown/keydown/scroll/touchstart, never on a timer: an idle-timeout fallback made the widget iframe land with no user input and register as a 0.047 layout shift on every page (2026-09-02). The two IDs still come from content.json via scripts/build-site-data.js, so this hash still changes whenever they change in /admin.",
      "emittedBy": "scripts/build-site-data.js (site.tawkToPropertyId / site.tawkToWidgetId markers)",
      "pages": "all 16 top-level pages"
    },
    "sha256-gUmq6gOoZ76O8R+C9JLLJzm46Aj4WygKv4n1nBxUfUk=": {
      "what": "Speculation-rules JSON that main.js builds at runtime (JSON.stringify of its prerender/prefetch rules object) and appends as <script type=\"speculationrules\">. It is never present in the static HTML, so the page scan cannot see it; 'runtime: true' keeps it in the emitted CSP as belt-and-braces beside the 'inline-speculation-rules' keyword. If the rules object in main.js changes, recompute this hash from JSON.stringify(rules).",
      "emittedBy": "assets/js/main.js (speculation rules block, appended to <body> at runtime)",
      "pages": "all pages, at runtime only",
      "runtime": true
    }
  }
}
