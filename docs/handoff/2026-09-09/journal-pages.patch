From e62fcbbd23cc5254cc307788af3d80816d558bfe Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Wed, 9 Sep 2026 20:43:12 +0000
Subject: [PATCH 1/2] WIP: per-post journal pages -- see HANDOFF-journal.md

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Qk26iqfYGbF5WXARWRCfDh
---
 assets/js/main.js     | 230 ++++++++++++++----------------------------
 assets/js/markdown.js | 221 ++++++++++++++++++++++++++++++++++++++++
 journal.html          |   4 +-
 sw.js                 |  11 ++
 4 files changed, 311 insertions(+), 155 deletions(-)
 create mode 100644 assets/js/markdown.js

diff --git a/assets/js/main.js b/assets/js/main.js
index cd7041e..75479a1 100644
--- a/assets/js/main.js
+++ b/assets/js/main.js
@@ -1116,157 +1116,37 @@
     return cleaned;
   }
 
-  /* ---------- shared: minimal, escape-first Markdown renderer ----------
-     The Apothecary Journal's post body is written by Savanna in /admin (the
-     `content` field is a rich-text editor there) and rendered on journal.html
-     through innerHTML, so this has exactly two jobs: cover the handful of
-     formatting marks a shop owner actually needs, and never let post text
-     become live markup.
-
-     Why this isn't a vendored library. Self-hosting one would have been fine
-     -- the site's CSP only blocks CDN scripts, and we already self-host the
-     fonts for that same reason (docs/SELF-HOSTING-FONTS.md) -- so this was a
-     trade, not a constraint:
-       - snarkdown (1.9 KB minified, MIT) is the closest fit by size, but its
-         last release was 2020, it passes raw HTML straight through (an
-         `<img src=x onerror=...>` in a post survives verbatim), writes hrefs
-         with no scheme check (`[x](javascript:alert(1))` becomes a live
-         link), and separates paragraphs with `<br />` instead of `<p>` --
-         which on its own would restyle every post already published, since
-         journal.html styles `.content p`. Fixing the first two means forking
-         its single dense minified regex, which throws away the reason to
-         vendor it in the first place.
-       - marked (40 KB minified) and markdown-it (124 KB minified) are each
-         bigger than every file this site ships except main.js itself, for a
-         page that renders a couple of posts. marked doesn't sanitize either
-         (its docs hand you off to DOMPurify); markdown-it is genuinely safe
-         by default (html:false plus a scheme allowlist) but is ~15x the size
-         of the ~8 KB below for the same handful of formatting marks.
-     So: escape with attrEsc() FIRST, then add formatting to text that can no
-     longer contain markup. Anything unsupported degrades to plain text. */
-
-  /* Inline emphasis. Only ever runs on text attrEsc() has already escaped,
-     so there is no "<" left for it to turn into a tag. Sveltia's editor
-     writes **bold** and _italic_; *italic* and __bold__ are accepted too
-     because that's what people type by hand. An underscore inside a word
-     (soap_batch_2) is not emphasis, which is why those two rules check the
-     characters on either side. */
-  function mdEmphasis(escaped) {
-    return escaped
-      .replace(/(^|[^A-Za-z0-9_])__([^\n]+?)__(?![A-Za-z0-9_])/g, "$1<strong>$2</strong>")
-      .replace(/\*\*([^\n]+?)\*\*/g, "<strong>$1</strong>")
-      .replace(/(^|[^A-Za-z0-9_])_([^_\n]+?)_(?![A-Za-z0-9_])/g, "$1<em>$2</em>")
-      .replace(/\*([^*\n]+?)\*/g, "<em>$1</em>");
-  }
-
-  /* [label](url). The URL may not contain whitespace, and may contain at most
-     one nested pair of parentheses -- enough for the Wikipedia-style
-     ".../Arnica_(plant)" links an herbal blog actually uses, while staying a
-     single unambiguous match per link (the two alternatives can't match the
-     same character, so there is nothing here to backtrack over). */
-  var MD_LINK_RE = /\[([^\]\n]*)\]\(((?:[^()\s]|\([^()\s]*\))*)\)/g;
-
-  /* One run of markdown text -> safe HTML. Escaping happens per slice so the
-     URL is checked in its raw form (before "&" becomes "&amp;") and only then
-     escaped for the attribute it lands in. */
-  function mdInline(text) {
-    var html = "";
-    var lastIndex = 0;
-    var match;
-    MD_LINK_RE.lastIndex = 0;
-    while ((match = MD_LINK_RE.exec(text)) !== null) {
-      html += mdEmphasis(attrEsc(text.slice(lastIndex, match.index)));
-      var href = safeLinkUrl(match[2]);
-      var label = mdEmphasis(attrEsc(match[1]));
-      // A rejected URL (javascript:, data:, ...) keeps the words and drops
-      // the link -- it never reaches an href.
-      html += href ? '<a href="' + attrEsc(href) + '">' + label + "</a>" : label;
-      lastIndex = MD_LINK_RE.lastIndex;
-    }
-    return html + mdEmphasis(attrEsc(text.slice(lastIndex)));
-  }
-
-  /* Block structure: blank-line separated paragraphs (what every post written
-     before this existed already is), "## " headings, "- " bullet lists,
-     "1. " numbered lists, and "***" dividers. Deliberately not a CommonMark
-     parser -- no tables, code blocks, blockquotes or images, all of which are
-     also switched off in the editor (see admin/config.yml's `buttons` and
-     `editor_components` for the journal `content` field). */
-  function renderMarkdown(text) {
-    if (text == null) return "";
-    var lines = String(text).replace(/\r\n?/g, "\n").split("\n");
-    var html = "";
-    var para = [];
-    var items = [];
-    var listTag = "";
-
-    function flushPara() {
-      if (!para.length) return;
-      // Joined with "\n", not " ": a plain-text post then renders the exact
-      // same bytes it did before this function existed.
-      html += "<p>" + mdInline(para.join("\n")) + "</p>";
-      para = [];
-    }
-
-    function flushList() {
-      if (!items.length) return;
-      html +=
-        "<" +
-        listTag +
-        ">" +
-        items
-          .map(function (item) {
-            return "<li>" + mdInline(item) + "</li>";
-          })
-          .join("") +
-        "</" +
-        listTag +
-        ">";
-      items = [];
-      listTag = "";
+  /* ---------- shared: Markdown renderer for journal post text ----------
+     ONE implementation, in assets/js/markdown.js, shared with the build:
+     scripts/build-site-data.js renders every post into its own static page
+     at /journal/<slug>.html through the same file, so the two can never
+     drift. journal.html loads it as a plain <script> before this one (it
+     publishes window.YL_MARKDOWN); in Node (the unit tests) it is
+     require()d. See that file for what the renderer does and does not
+     support, and why it is not a vendored library. */
+  function markdownModule() {
+    if (window.YL_MARKDOWN) return window.YL_MARKDOWN;
+    if (typeof module !== "undefined" && module.exports && typeof require === "function") {
+      return require("./markdown.js");
     }
+    return null;
+  }
 
-    function pushItem(tag, item) {
-      // A "1." right after a "-" starts a second, differently-tagged list.
-      if (listTag && listTag !== tag) flushList();
-      flushPara();
-      listTag = tag;
-      items.push(item);
-    }
-
-    lines.forEach(function (line) {
-      var trimmed = line.trim();
-      var heading = /^(#{1,6})\s+(.+)$/.exec(trimmed);
-      var bullet = /^[-*+]\s+(.+)$/.exec(trimmed);
-      var numbered = /^\d{1,9}[.)]\s+(.+)$/.exec(trimmed);
-
-      if (!trimmed) {
-        flushPara();
-        flushList();
-      } else if (/^(\*{3,}|-{3,}|_{3,})$/.test(trimmed)) {
-        flushPara();
-        flushList();
-        html += "<hr>";
-      } else if (heading) {
-        flushPara();
-        flushList();
-        // The post's own title is the page's <h2>, so headings inside a post
-        // start at <h3> and never skip a level (screen-reader outline).
-        var tag = heading[1].length > 2 ? "h4" : "h3";
-        html += "<" + tag + ">" + mdInline(heading[2]) + "</" + tag + ">";
-      } else if (bullet) {
-        pushItem("ul", bullet[1]);
-      } else if (numbered) {
-        pushItem("ol", numbered[1]);
-      } else {
-        flushList();
-        para.push(line);
-      }
-    });
-
-    flushPara();
-    flushList();
-    return html;
+  function renderMarkdown(text) {
+    var md = markdownModule();
+    if (md) return md.renderMarkdown(text);
+    /* A page that loads this file without markdown.js (only journal.html
+       ships it) renders the post as the plain, escaped paragraphs every post
+       was before the editor grew a formatting toolbar. Escaping is the part
+       that must never be skipped; the formatting is the part that can be. */
+    if (text == null) return "";
+    return String(text)
+      .replace(/\r\n?/g, "\n")
+      .split("\n\n")
+      .map(function (p) {
+        return "<p>" + attrEsc(p) + "</p>";
+      })
+      .join("");
   }
 
   /* ---------- shared: horizontal swipe gesture ----------
@@ -8060,6 +7940,13 @@
     var journalPosts = window.YL_JOURNAL.posts || [];
     var currentJournalTagFilter = null;
 
+    /* Each post's own static page (see routeJournal below for the old
+       `#post-<slug>` address). Root-absolute so the same string is right from
+       journal.html and from anywhere the search results render it. */
+    function postPageUrl(post) {
+      return "/journal/" + encodeURIComponent(post.id) + ".html";
+    }
+
     function renderJournalList() {
       /* Switched off, or on with nothing written yet: both get the same
          "coming soon" notice. The page is kept out of the nav, out of
@@ -8122,8 +8009,8 @@
               attrEsc(readTime) +
               "</span>" +
               "  </div>" +
-              '  <h3><a href="#post-' +
-              attrEsc(post.id) +
+              '  <h3><a href="' +
+              attrEsc(postPageUrl(post)) +
               '">' +
               attrEsc(post.title) +
               "</a></h3>" +
@@ -8133,8 +8020,8 @@
               "</p>" +
               '  <div class="card-foot">' +
               '    <div class="card-foot-row">' +
-              '      <a href="#post-' +
-              attrEsc(post.id) +
+              '      <a href="' +
+              attrEsc(postPageUrl(post)) +
               '" class="btn btn-outline btn-sm">Read Post →</a>' +
               "    </div>" +
               "  </div>" +
@@ -8235,11 +8122,46 @@
       wireReveal(journalApp);
     }
 
+    /* Every published post has its own static page at /journal/<slug>.html
+       (built by scripts/build-site-data.js), which is what the cards, the
+       feed, the sitemap and the JSON-LD all link to. The old in-page
+       `#post-<slug>` address is kept alive for anyone who bookmarked or
+       shared one: a hash naming a published post is replaced with the
+       page's real URL (replace, not assign, so Back does not bounce through
+       the fragment). Only a hash for a post that has no page -- a typo, or a
+       post that no longer exists -- is rendered here, and that path shows
+       the list. `#tag=<topic>` is the address the static pages' topic pills
+       link to; it opens the list already filtered. */
+    function decodeHashPart(part) {
+      // A malformed escape ("%E0") throws; treat it as the literal text.
+      try {
+        return decodeURIComponent(part);
+      } catch {
+        return part;
+      }
+    }
+
+    function staticPostUrl(postId) {
+      var post = journalPosts.find(function (p) {
+        return p.id === postId;
+      });
+      return post && enableJournal ? "/journal/" + encodeURIComponent(post.id) + ".html" : "";
+    }
+
     function routeJournal() {
       var hash = window.location.hash || "";
       if (hash.indexOf("#post-") === 0) {
-        var postId = hash.replace("#post-", "");
+        var postId = decodeHashPart(hash.replace("#post-", ""));
+        var target = staticPostUrl(postId);
+        if (target) {
+          window.location.replace(target);
+          return;
+        }
         renderJournalDetail(postId);
+      } else if (hash.indexOf("#tag=") === 0) {
+        var tag = decodeHashPart(hash.slice("#tag=".length));
+        currentJournalTagFilter = tag || null;
+        renderJournalList();
       } else {
         renderJournalList();
       }
diff --git a/assets/js/markdown.js b/assets/js/markdown.js
new file mode 100644
index 0000000..d8cc140
--- /dev/null
+++ b/assets/js/markdown.js
@@ -0,0 +1,221 @@
+/**
+ * @fileoverview The ONE Markdown renderer for Apothecary Journal post text.
+ *
+ * A post's `content` is written by Savanna in /admin (the field is a rich-text
+ * editor that stores Markdown) and is drawn in two places: by the build
+ * (scripts/build-site-data.js renders every post into its own static page at
+ * /journal/<slug>.html) and by the browser (assets/js/main.js, which still
+ * renders a post in place on journal.html when a `#post-<slug>` link cannot be
+ * redirected). Two copies of this function would drift the first time one of
+ * them was edited, so both load THIS file: the build `require()`s it and
+ * journal.html loads it as a plain <script> that publishes `window.YL_MARKDOWN`.
+ * scripts/markdown.test.js asserts the two entry points are the same function
+ * and that every published post renders identically through each.
+ *
+ * The renderer has exactly two jobs: cover the handful of formatting marks a
+ * shop owner actually needs, and never let post text become live markup.
+ *
+ * Why this isn't a vendored library. Self-hosting one would have been fine --
+ * the site's CSP only blocks CDN scripts, and we already self-host the fonts
+ * for that same reason (docs/SELF-HOSTING-FONTS.md) -- so this was a trade,
+ * not a constraint:
+ *   - snarkdown (1.9 KB minified, MIT) is the closest fit by size, but its
+ *     last release was 2020, it passes raw HTML straight through (an
+ *     `<img src=x onerror=...>` in a post survives verbatim), writes hrefs
+ *     with no scheme check (`[x](javascript:alert(1))` becomes a live link),
+ *     and separates paragraphs with `<br />` instead of `<p>` -- which on its
+ *     own would restyle every post already published, since the journal
+ *     styles `.content p`. Fixing the first two means forking its single
+ *     dense minified regex, which throws away the reason to vendor it.
+ *   - marked (40 KB minified) and markdown-it (124 KB minified) are each
+ *     bigger than every file this site ships except main.js itself, for a
+ *     page that renders a couple of posts. marked doesn't sanitize either
+ *     (its docs hand you off to DOMPurify); markdown-it is genuinely safe by
+ *     default (html:false plus a scheme allowlist) but is ~15x the size of
+ *     the ~8 KB below for the same handful of formatting marks.
+ * So: escape FIRST, then add formatting to text that can no longer contain
+ * markup. Anything unsupported degrades to plain text.
+ */
+(function (root, factory) {
+  if (typeof module === "object" && module.exports) {
+    module.exports = factory();
+  } else {
+    root.YL_MARKDOWN = factory();
+  }
+})(typeof self !== "undefined" ? self : this, function () {
+  "use strict";
+
+  /* Same table as attrEsc() in main.js and escapeHtml() in the build: the
+     output lands in text nodes AND attribute values (href), so quotes and
+     backticks are escaped too. */
+  function escapeHtml(str) {
+    if (str == null) return "";
+    return String(str)
+      .replace(/&/g, "&amp;")
+      .replace(/</g, "&lt;")
+      .replace(/>/g, "&gt;")
+      .replace(/"/g, "&quot;")
+      .replace(/'/g, "&#39;")
+      .replace(/`/g, "&#96;");
+  }
+
+  /* Strips every control character and space BEFORE deciding what the scheme
+     is, so " javascript:alert(1)" and "java<TAB>script:alert(1)" are both
+     caught. Only http, https and mailto may carry a scheme; a relative
+     reference ("shop.html", "/journal.html", "#gift-cards") cannot execute
+     and passes through. Identical to safeLinkUrl() in main.js. */
+  function safeLinkUrl(url) {
+    if (!url) return "";
+    var raw = String(url);
+    var cleaned = "";
+    for (var i = 0; i < raw.length; i++) {
+      var code = raw.charCodeAt(i);
+      if (code > 32 && code !== 127) cleaned += raw.charAt(i);
+    }
+    if (!cleaned) return "";
+    var scheme = /^([a-zA-Z][a-zA-Z0-9+.-]*):/.exec(cleaned);
+    if (scheme) {
+      var name = scheme[1].toLowerCase();
+      if (name !== "http" && name !== "https" && name !== "mailto") return "";
+      return cleaned;
+    }
+    return cleaned;
+  }
+
+  /* Inline emphasis. Only ever runs on text escapeHtml() has already escaped,
+     so there is no "<" left for it to turn into a tag. Sveltia's editor
+     writes **bold** and _italic_; *italic* and __bold__ are accepted too
+     because that's what people type by hand. An underscore inside a word
+     (soap_batch_2) is not emphasis, which is why those two rules check the
+     characters on either side. */
+  function mdEmphasis(escaped) {
+    return escaped
+      .replace(/(^|[^A-Za-z0-9_])__([^\n]+?)__(?![A-Za-z0-9_])/g, "$1<strong>$2</strong>")
+      .replace(/\*\*([^\n]+?)\*\*/g, "<strong>$1</strong>")
+      .replace(/(^|[^A-Za-z0-9_])_([^_\n]+?)_(?![A-Za-z0-9_])/g, "$1<em>$2</em>")
+      .replace(/\*([^*\n]+?)\*/g, "<em>$1</em>");
+  }
+
+  /* [label](url). The URL may not contain whitespace, and may contain at most
+     one nested pair of parentheses -- enough for the Wikipedia-style
+     ".../Arnica_(plant)" links an herbal blog actually uses, while staying a
+     single unambiguous match per link (the two alternatives can't match the
+     same character, so there is nothing here to backtrack over). */
+  var MD_LINK_RE = /\[([^\]\n]*)\]\(((?:[^()\s]|\([^()\s]*\))*)\)/g;
+
+  /* One run of markdown text -> safe HTML. Escaping happens per slice so the
+     URL is checked in its raw form (before "&" becomes "&amp;") and only then
+     escaped for the attribute it lands in. */
+  function mdInline(text) {
+    var html = "";
+    var lastIndex = 0;
+    var match;
+    MD_LINK_RE.lastIndex = 0;
+    while ((match = MD_LINK_RE.exec(text)) !== null) {
+      html += mdEmphasis(escapeHtml(text.slice(lastIndex, match.index)));
+      var href = safeLinkUrl(match[2]);
+      var label = mdEmphasis(escapeHtml(match[1]));
+      // A rejected URL (javascript:, data:, ...) keeps the words and drops
+      // the link -- it never reaches an href.
+      html += href ? '<a href="' + escapeHtml(href) + '">' + label + "</a>" : label;
+      lastIndex = MD_LINK_RE.lastIndex;
+    }
+    return html + mdEmphasis(escapeHtml(text.slice(lastIndex)));
+  }
+
+  /* Block structure: blank-line separated paragraphs (what every post written
+     before this existed already is), "## " headings, "- " bullet lists,
+     "1. " numbered lists, and "***" dividers. Deliberately not a CommonMark
+     parser -- no tables, code blocks, blockquotes or images, all of which are
+     also switched off in the editor (see admin/config.yml's `buttons` and
+     `editor_components` for the journal `content` field).
+
+     options.headingLevel is the tag a "##" heading becomes (a "###" or deeper
+     heading is one level below it, and nothing goes past <h6>). It defaults
+     to 3 because on journal.html the post title is the page's <h2>, so
+     headings inside a post start at <h3> and never skip a level; the static
+     post pages put the title in an <h1> and pass 2 for the same reason. */
+  function renderMarkdown(text, options) {
+    if (text == null) return "";
+    var base = options && options.headingLevel ? Number(options.headingLevel) : 3;
+    if (!(base >= 1 && base <= 6)) base = 3;
+    var lines = String(text).replace(/\r\n?/g, "\n").split("\n");
+    var html = "";
+    var para = [];
+    var items = [];
+    var listTag = "";
+
+    function flushPara() {
+      if (!para.length) return;
+      // Joined with "\n", not " ": a plain-text post then renders the exact
+      // same bytes it did before this function existed.
+      html += "<p>" + mdInline(para.join("\n")) + "</p>";
+      para = [];
+    }
+
+    function flushList() {
+      if (!items.length) return;
+      html +=
+        "<" +
+        listTag +
+        ">" +
+        items
+          .map(function (item) {
+            return "<li>" + mdInline(item) + "</li>";
+          })
+          .join("") +
+        "</" +
+        listTag +
+        ">";
+      items = [];
+      listTag = "";
+    }
+
+    function pushItem(tag, item) {
+      // A "1." right after a "-" starts a second, differently-tagged list.
+      if (listTag && listTag !== tag) flushList();
+      flushPara();
+      listTag = tag;
+      items.push(item);
+    }
+
+    lines.forEach(function (line) {
+      var trimmed = line.trim();
+      var heading = /^(#{1,6})\s+(.+)$/.exec(trimmed);
+      var bullet = /^[-*+]\s+(.+)$/.exec(trimmed);
+      var numbered = /^\d{1,9}[.)]\s+(.+)$/.exec(trimmed);
+
+      if (!trimmed) {
+        flushPara();
+        flushList();
+      } else if (/^(\*{3,}|-{3,}|_{3,})$/.test(trimmed)) {
+        flushPara();
+        flushList();
+        html += "<hr>";
+      } else if (heading) {
+        flushPara();
+        flushList();
+        var level = Math.min(6, heading[1].length > 2 ? base + 1 : base);
+        var tag = "h" + level;
+        html += "<" + tag + ">" + mdInline(heading[2]) + "</" + tag + ">";
+      } else if (bullet) {
+        pushItem("ul", bullet[1]);
+      } else if (numbered) {
+        pushItem("ol", numbered[1]);
+      } else {
+        flushList();
+        para.push(line);
+      }
+    });
+
+    flushPara();
+    flushList();
+    return html;
+  }
+
+  return {
+    renderMarkdown: renderMarkdown,
+    escapeHtml: escapeHtml,
+    safeLinkUrl: safeLinkUrl
+  };
+});
diff --git a/journal.html b/journal.html
index ce7f5eb..b9d7798 100644
--- a/journal.html
+++ b/journal.html
@@ -157,7 +157,8 @@
   .journal-detail .content { line-height: 1.8; font-size: 1.1rem; }
   .journal-detail .content p { margin-bottom: 1.5em; }
   /* Formatting Savanna can add from /admin (see the journal "content" field in
-     admin/config.yml, rendered by renderMarkdown() in assets/js/main.js). The
+     admin/config.yml, rendered by assets/js/markdown.js -- the same file the
+     build uses for the static /journal/<slug>.html pages). The
      post's own title is the <h2> above, so headings inside a post start at
      <h3>. Lists need their markers back -- styles.css resets every ul. */
   .journal-detail .content h3 { font-family: var(--font-display); font-size: 1.6rem; margin: 1.8em 0 0.5em; }
@@ -379,6 +380,7 @@
 <script src="assets/js/journal-data.js?v=2.0" defer></script>
 <script src="assets/js/social-feed-data.js?v=2.0" defer></script>
 <script src="assets/js/search-data.js?v=2.0" defer></script>
+<script src="assets/js/markdown.js?v=2.0" defer></script>
 <script src="assets/js/main.js?v=2.0" defer></script>
 <script src="assets/js/cart.js" defer></script>
 <!-- Live chat (Tawk.to) -- placeholder, inert until real IDs are set.
diff --git a/sw.js b/sw.js
index 8c16b91..b690c67 100644
--- a/sw.js
+++ b/sw.js
@@ -48,6 +48,10 @@ const ASSETS_TO_CACHE = [
   '/assets/css/styles.css',
   '/assets/css/cart.css',
   '/assets/js/main.js',
+  // The journal post renderer (see assets/js/markdown.js); journal.html loads
+  // it before main.js. The static /journal/<slug>.html pages are deliberately
+  // NOT precached -- see the note on the runtime handler below.
+  '/assets/js/markdown.js',
   '/assets/js/porch-light.js',
   '/assets/js/cart.js',
   '/assets/js/thank-you.js',
@@ -302,6 +306,13 @@ self.addEventListener('fetch', event => {
     if (isNavigation || isCodeAsset) {
       // Network-First strategy for HTML and code assets: prefer live server data when online,
       // fall back to cache only when offline or connection is lost.
+      //
+      // This branch is also what serves the generated journal post pages
+      // (/journal/<slug>.html) and product pages (/products/<id>.html):
+      // neither set is in ASSETS_TO_CACHE -- one page per post would grow the
+      // install budget with every article written -- so a post a shopper has
+      // opened is cached here on the way through, and one they have not opened
+      // gets /offline.html below, exactly as a product page does.
       event.respondWith(
         (async () => {
           try {
-- 
2.43.0


From 61e93db126f9abe0c2f10c767950824f627f5deb Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Wed, 9 Sep 2026 20:55:21 +0000
Subject: [PATCH 2/2] WIP: journal-pages (agent stopped by usage limit; see
 docs/HANDOFF-2026-09-09.md)

---
 HANDOFF-journal.md | 162 +++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 162 insertions(+)
 create mode 100644 HANDOFF-journal.md

diff --git a/HANDOFF-journal.md b/HANDOFF-journal.md
new file mode 100644
index 0000000..5f06c67
--- /dev/null
+++ b/HANDOFF-journal.md
@@ -0,0 +1,162 @@
+# Handoff: per-post Journal pages (`journal/<slug>.html`)
+
+Worktree: `/home/user/yallternative-living/.claude/worktrees/agent-aab3d8b8e4939c0dd`
+Branch: `worktree-agent-aab3d8b8e4939c0dd` (merged from `claude/website-world-class-gaps-ox05mw`; `npm ci` done)
+Stopped mid-implementation on coordinator instruction. Nothing has been pushed.
+
+## Done (uncommitted work is now in commit "WIP: per-post journal pages")
+
+Verified by reading only -- NO test suite has been run since these edits. `npm run lint`,
+`npm run format:check`, `npm test` etc. are all still owed.
+
+1. `assets/js/markdown.js` (NEW) -- the single Markdown renderer, UMD: `module.exports` in
+   Node, `window.YL_MARKDOWN` in the browser. Exports `renderMarkdown(text, {headingLevel})`,
+   `escapeHtml`, `safeLinkUrl`. Byte-identical port of the old `renderMarkdown`/`mdInline`/
+   `mdEmphasis`/`MD_LINK_RE` from `main.js`, plus one option: `headingLevel` (default 3 =
+   old behaviour: `##` -> h3, `###` -> h4; the static pages should pass 2 because their title
+   is an `<h1>`).
+2. `assets/js/main.js`:
+   - old markdown block (was lines 1119-1271) replaced by `markdownModule()` +
+     `renderMarkdown()` that delegate to `window.YL_MARKDOWN` (browser) or
+     `require("./markdown.js")` (Node); fallback = escaped plain `<p>` paragraphs.
+     `main.renderMarkdown` is still exported, so `scripts/main.test.js` should still pass.
+   - Journal cards now link to `postPageUrl(post)` = `/journal/<encoded id>.html` (both the
+     `<h3>` link and the "Read Post" button).
+   - `routeJournal()`: `#post-<id>` for a published post -> `window.location.replace("/journal/<id>.html")`;
+     unknown id -> old in-page `renderJournalDetail` (which falls back to the list);
+     new `#tag=<topic>` hash opens the list pre-filtered (`decodeHashPart` guards bad escapes).
+   - `renderJournalDetail`, `renderFeaturedProductCardHtml`, `findFeaturedProduct`,
+     `getReadingTime`, `renderJournalTagsHtml` are UNCHANGED (still exported; `journal-engine.test.js` uses them).
+3. `journal.html`: `<script src="assets/js/markdown.js?v=2.0" defer>` added before `main.js`;
+   inline-CSS comment updated to point at markdown.js.
+4. `sw.js`: `'/assets/js/markdown.js'` added to `ASSETS_TO_CACHE` (after main.js) with a note;
+   comment on the network-first navigation branch says `/journal/*.html` is served there and
+   never precached. CACHE_NAME will be re-digested by the build.
+
+## Not started (in order)
+
+1. **Build (`scripts/build-site-data.js`)** -- nothing written yet. Plan:
+   - `const md = require("../assets/js/markdown.js")` at top; use `md.renderMarkdown(post.content, {headingLevel: 2})`.
+   - Add `active` option to `renderSiteHeaderHtml(manifest, opts)` (line ~7249): Shop link is
+     hard-coded `class="active"`; journal pages need Journal active instead.
+   - New helpers (put near `generateJournalJsonLd`, ~line 7757): `journalPostUrl(post, dom)`
+     (`dom + "/journal/" + encodeURIComponent(id) + ".html"`), `formatJournalDate` (fixed month
+     names, no locale), `journalPostingNode(post, dom, url)` factored OUT of
+     `generateJournalJsonLd` so Blog.blogPost URLs and the per-page BlogPosting share one node
+     (page node adds `@context`, `mainEntityOfPage: {"@type":"WebPage","@id":url}`,
+     `isPartOf: {"@type":"Blog","@id": dom+"/journal.html#blog"}`), `generateJournalBreadcrumbJsonLd`
+     (Home > Journal > title), `renderJournalTagLinksHtml` (anchors to `/journal.html#tag=<enc>`,
+     class `journal-tag journal-tag-pill`, all escaped), `renderJournalFeaturedCardHtml(product,
+     categoryLabel, manifest)` (mirror of main.js markup but link to `/products/<id>.html`; use
+     h2/h3 not h4/h5 -- axe `heading-order` is in the a11y gate), `renderJournalPostHtml(post,
+     journal, domain, ctx)` mirroring `renderProductPdpHtml` head (title = `<post title> | Y'allternative Living`,
+     meta description = `truncateForMeta(excerpt,155)`, canonical, og:type=article,
+     og/twitter image = `domain + "/" + rasterImagePath(post.image,"social")` or `SITE_OG_IMAGE`,
+     `<link rel="alternate" rss>`, `preloadFromManifest(post.image,...)`, both JSON-LD blocks via
+     `JSON.stringify(...).replace(/<\//g,"<\\/")` like the PDP), body = skip link, header
+     (`{enableJournal:true, active:"journal"}`), breadcrumb nav, `<article class="journal-detail">`
+     with `<a class="back-link" id="journalBackBtn" href="/journal.html">`, `<h1>`, `.meta`
+     (`<time datetime>` + clock svg + reading time = `post.readingTime || post.readTime`),
+     `.journal-detail-tags`, `pictureFromManifest(post.image, manifest, {alt:title, sizes:"(max-width: 820px) 100vw, 800px"})`,
+     `.content` body, featured card, newer/older pager, then `renderGlobalSearchModalHtml(ctx.search)`,
+     footer, scripts (content-data, products-data, events-data, search-data, image-manifest, main.js, cart.js),
+     `renderTawkChatHtml(ctx.site)`. Copy journal.html's inline `<style>` block (lines ~150-175)
+     into the page and add rules for `.content h2`, `.journal-detail-tags .journal-tag` (anchor), `.journal-pager`.
+   - Guard: `post.id` must match `/^[A-Za-z0-9][A-Za-z0-9._-]*$/` (section "5. Journal post guards", ~line 1967).
+   - Generation step next to `generateProductOgPages` (~line 5284): hoist `pdpManifest` +
+     `pdpFooterInner` so both loops share them; when `SITE_CONFIG.enableJournal` write
+     `journal/<id>.html` for each `JOURNAL.posts[i]` (newer = posts[i-1], older = posts[i+1]),
+     run `setFormAction(html,"footer-signup-form",newsletterAction(...))` like the PDP; ALWAYS
+     prune `journal/*.html` not in the expected set (all of them when the flag is off; rmdir if empty).
+   - Repoint URLs: `generateJournalJsonLd` blogPost (~7778), `generateRssFeed` item link/guid (~7833),
+     search index `url: "journal.html#post-"` (~2528 -> `"journal/"+id+".html"`), llms.txt
+     `journalLines` (~4711, add the URL), sitemap (~4557: add an entry per post when enabled,
+     `lastmod = post.date.slice(0,10)`, priority 0.6 -- NOT git-derived), `collectBuiltHtml`
+     (~806: also glob `journal/`), `cleanAttributeMarkers` list (~5346).
+   - Wording: add to `assets/data/content.json` `journal`: `backLabel` ("← Back to Journal"),
+     `newerLabel` ("Newer post"), `olderLabel` ("Older post"); read them in `loadJournal`
+     (~180) with those defaults.
+2. **`scripts/build-security-headers.js`**: `JOURNAL_PATHS` (line 133) add `"/journal/*"`
+   (Netlify splat, rendered as a 404 redirect rule at ~908); `allPages()` (~261) also glob
+   `journal/*.html` for the inline-script hash check. `scripts/qa-check.js` `checkJournalGate`
+   (~4845) must add `"/journal/*"` to its list or the two disagree.
+3. **`admin/config.yml`**: journal collection (line 162) -- extend `description` with:
+   "Each post gets its own page at /journal/<slug>.html. The slug is made from the title the
+   first time you save (lowercase, words joined with dashes, accents stripped) and never
+   changes afterwards, so links keep working even if you retitle the post." Slug config is
+   already `slug: "{{slug}}"` + top-level `slug: clean_accents: true` (line 128) -- fine, no
+   change. Fix the comment at ~line 180 that says "renderMarkdown() in assets/js/main.js" ->
+   markdown.js. Add the three wording fields (with hints) under Site Settings -> "Journal page"
+   object (line 2160).
+4. **`docs/EDITING-GUIDE.md`** Walkthrough 6 C (line 313): add a step saying every post gets
+   its own page at /journal/<slug>.html, how the slug is chosen, and that the Back / Newer /
+   Older labels live under Site Settings -> Journal page.
+5. **Gates**:
+   - `scripts/qa-check.js`: every block that globs `products/` (lines 955, 1026, 3268, 4364,
+     4732, 4920, 5204) should also glob `journal/`; add a journal twin of the PDP block at
+     ~3387 (self-canonical, BlogPosting + BreadcrumbList JSON-LD parse, `mainEntityOfPage`
+     = own URL, no noindex, no `window.location.replace`, image exists on disk).
+   - `scripts/build-site-data.test.js`: tests for `renderJournalPostHtml` (title/canonical/
+     JSON-LD/escaping of `<script>` in title, tags, excerpt), markdown parity (build output ==
+     `md.renderMarkdown` for every `loadJournal().posts[]`, and `require("../assets/js/main.js").renderMarkdown`
+     === module output), prune-when-off (needs a tmp ROOT or a pure function `journalPagesToPrune(existing, expected)`).
+   - NEW `scripts/markdown.test.js` (unit pool auto-globs `*.test.js`): move/duplicate the
+     XSS cases from `main.test.js` lines 321-400 against `assets/js/markdown.js` and assert
+     `headingLevel:2` gives `<h2>`/`<h3>`. Suite count in README/AGENTS/TEST_INFRA becomes 49.
+   - `scripts/a11y-check.js` `collectPages()` (line 97): also glob `journal/`; then RUN the
+     gate and add measured `INCOMPLETE_BASELINE` entries for `journal/<slug>.html [dark|light]`
+     (default is 0 and the header/search modal alone produce ~11-17 incompletes per page).
+   - `scripts/verify-build-reproducibility.js` `targetDirs` (line 33): add `journal/`.
+   - `scripts/smoke-test.js` (~605) and `scripts/build-security-headers.test.js`: check for
+     product-page lists that should include journal pages.
+   - Browser suites that drive the old hash view and WILL now follow the redirect:
+     `scripts/m4-adversarial-challenger.browser.test.js` lines 268, 439 (`window.location.hash =
+     "#post-small-batch-difference"` on what is now a static page -- change to `page.goto(
+     ".../journal.html#post-small-batch-difference")`), 780-806 (card click -> now a real
+     navigation; `#journalBackBtn` is an `<a>` on the static page -> wait for navigation, then
+     assert cards); `scripts/challenger-m4-stress.browser.test.js` line 860 (`.journal-detail h2`
+     -> `h1`). Keep the assertions, change only the navigation mechanics.
+6. **Docs**: README.md lines 61-76, AGENTS.md rows 1/4 and the a11y note (37 pages / 74 scans
+   -> 39 / 78 with the two current posts; say "plus one page per journal post"), TEST_INFRA.md
+   line 20 (already stale: PDPs are not doorways) and 830.
+7. **Verify** (in this order): `npm run build-data` (also runs prettier), `node scripts/build-security-headers.js`,
+   `npm run format`, `npm run lint`, `npm run format:check`, `npm test`, `npm run test:smoke`,
+   `node scripts/m4-adversarial-challenger.browser.test.js` (three local Playwright-binary
+   failures are expected), `node scripts/extended_qa_test.js`, `node scripts/a11y-check.js`.
+   Then squash into one clean commit (no model names anywhere).
+
+## Known traps
+
+- **Reproducibility**: five rebuilds must be byte-identical. Never use `new Date()` / wall
+  clock in the pages; sitemap `lastmod` for posts = the post's own date; `feed.xml`
+  `lastBuildDate` already derives from the newest post. `sw.js` CACHE_NAME is a digest of the
+  precache list -- adding markdown.js changes it once, which is expected.
+- **qa-check.js page lists**: several checks assert "found N pages" with a floor (e.g.
+  `chromePages.length < 30`); new pages only help, but any check that pins a count will move.
+  `sw.js: all N cached assets exist on disk` will fail until `assets/js/markdown.js` is
+  committed (it is) and reachable at `/assets/js/markdown.js`.
+- **a11y baselines**: `INCOMPLETE_BASELINE_DEFAULT` is 0 and the gate fails on growth; the
+  numbers must be read off a real run. Headings: title h1, body h2/h3 (`headingLevel: 2`),
+  featured card h2/h3 -- do not copy the client's h4/h5.
+- **404 gating**: `build-security-headers.js` `BLOCKED_PATHS` becomes redirect rules in
+  `netlify.toml`; `qa-check.js checkJournalGate` asserts both directions (flag on + rule
+  present fails too). Add `/journal/*` in BOTH places in the same commit.
+- **Markdown sharing**: ONE implementation in `assets/js/markdown.js`; `main.js` only
+  delegates. Do not re-add a renderer to the build -- `require("../assets/js/markdown.js")`.
+  `.eslintrc.json` gives `assets/js/**` the browser env, and the module is written to lint
+  clean there (`typeof module` guard, `self`/`this` root) -- run `npm run lint` before
+  trusting that. `admin/config.yml` and `journal.html` comments referencing the old
+  `renderMarkdown() in main.js` location: journal.html fixed, config.yml still owed.
+- **Redirect + offline**: the SW no longer has an in-page detail to fall back on; an unvisited
+  post offline shows offline.html (accepted).
+- `products/*.html` are NOT pruned by the build today despite the task wording; implement
+  journal pruning from scratch (readdir `journal/`, unlink non-expected `*.html`).
+- `getReadingTime` on the client counts content + excerpt; the build's step 8 (~2218)
+  writes `post.readTime` from content only -- the page should use `post.readingTime || post.readTime`.
+
+## Owed CMS / guide items (none done yet)
+
+- `admin/config.yml`: collection description hint (text above), `renderMarkdown` comment
+  fix, three wording fields under Site Settings -> Journal page with hints.
+- `assets/data/content.json` `journal.backLabel/newerLabel/olderLabel`.
+- `docs/EDITING-GUIDE.md` Walkthrough 6 C.
-- 
2.43.0

